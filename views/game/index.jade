extends ../layout

block content
  h1= title
  p Welcome to #{title}
  input(type='hidden', value='#{ip}', id="hiddenIp")
  div
    p life : #{data.life} pv
    p name : #{data.name}
    p class : #{data.class}

